#include<iostream>
using namespace std;
int main(void){
    cout<<"NAMAste DUniya:-)"<<endl;
    unsigned int a=-101;
    
    cout<<a<<endl;
    cout<<sizeof(a)<<endl;
    /* process of assigning the negative value to a variable.
    1.)convert into corresponding binary digit
    2.)assign the last bits to the number and other with 000s
    3.)take 2s compliment :take 1s compliment by change 0s into 1 and 1 into binary of the stored binary then add 1 to it
    4.)initial 1 are represent the negative numbers.and 0 represent the positive number. */
}
